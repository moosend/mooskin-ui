class TextAreaExample extends React.Component {

    constructor(){
        super();
        this.onChange = this.onChange.bind(this);
    }

    render(){
        return (
            <div>
                <TextArea onChange={this.onChange} value="with value and custom rows/cols" label="Label:" rows={7} cols={35}/>
                <hr/>
                <TextArea onChange={this.onChange} disabled value="disabled" description="This text area is disabled"/>
            </div>
        );
    }

    onChange(e, data) {
        console.log(e.target);
    }
}
