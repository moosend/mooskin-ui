class InputExample extends React.Component {

    constructor(){
        super();
        this.onChange = this.onChange.bind(this);
        this.validate = this.validate.bind(this);

        this.state = {
            status: '',
            message: '',
            value: '',
            icon: 'search'
        }
    }

    render(){
        return(
            <div>
                <Input onChange={this.onChange} type="text" maxlength={5} placeholder="max length 5" label="Some label:"/>
                <br/>
                <Input
                    onChange={this.onChange}
                    value="password"
                    type="password"
                    label="Password:"
                    labelTop
                />
                <br/>
                <Input value={this.state.value} validate={this.validate} icon={this.state.icon} status={this.state.status} description={this.state.message} onChange={this.onChange} placeholder="placeholder" className=".input-field" autocomplete/>
                <br/>
                <Input onChange={this.onChange} disabled value="disabled" description="This input is disabled"/>
            </div>
        );
    }

    onChange(e, data) {
        console.log(e.target);
        this.setState({value: data.value})
    }

    validate(data){
        console.log(data.value);
        if (data.value){
            if (data.value.length < 5){
                this.setState({status: 'error', message: 'Input should have 5 or more characters', icon: 'clear'});
            } else {
                this.setState({status: 'success', message: '', icon: 'check'})
            }
        } else {
            this.setState({status: 'error', message: 'This input field is required', icon: 'clear'});
        }
    }

}
